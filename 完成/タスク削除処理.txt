◇基本手順

システムはデータベースに接続する。
システムは送られてきたidのデータをデータベースから削除する。
システムはデータベースとの接続を閉じる。
システムはトップページに遷移する。

データベース側の以上による処理中断。
トランザクション処理でカバーする。
エラーの場合はエラーメッセージを添えて詳細ページに遷移。

削除タスクのあったページを記憶して遷移。


◇代替手順

ラストページなどでタスクがあったページがなくなっていた場合はひとつ前のページに遷移。